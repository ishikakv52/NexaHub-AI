from django.apps import AppConfig


class EmailassistantConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "EmailAssistant"
    verbose_name = "AI Email Assistant"
